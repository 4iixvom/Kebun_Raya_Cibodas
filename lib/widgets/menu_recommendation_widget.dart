import 'package:flutter/material.dart';

class MenuRecommendationsWidget extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        'Rekomendasi Menu: Sate Maranggi, Nasi Liwet, Kue Mochi.',
        style: TextStyle(fontSize: 16),
      ),
    );
  }
}